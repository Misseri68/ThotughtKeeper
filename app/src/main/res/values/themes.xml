<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ThotKeeper" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">casual</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/darkViolet</item>
        <item name="colorAccent">@color/red</item>
        <item name="editTextStyle">@style/EditTextRoundedStyle</item>
    </style>

    <!-- Tema para actividades con ActionBar -->
    <style name="Theme.ThotKeeper.ConActionBar" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Hereda de Base.Theme.ThotKeeper y añade el ActionBar. Lo tengo que poner en el AndroidManifest para que solo lo usen las que necesitan la barra.-->
        <item name="android:fontFamily">casual</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/darkViolet</item>
        <item name="colorAccent">@color/red</item>
        <item name="editTextStyle">@style/EditTextRoundedStyle</item>
    </style>

    <style name="EditTextRoundedStyle" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edittext_redondo</item>
    </style>

    <style name="BarraMenu" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="iconTint">@color/salmon</item>


    </style>
</resources>
